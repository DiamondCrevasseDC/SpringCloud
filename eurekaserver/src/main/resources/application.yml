server:
  port: 8761
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    register-with-eureka: false #表示是不是client,防止服务端将自己注册成客户端
    fetch-registry: false #表示是不是client,防止服务端将自己注册成客户端
    service-url:
      default-zone: http://127.0.0.1:8761/eureka/

